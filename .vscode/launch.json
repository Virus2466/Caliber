{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Caliber",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Caliber.exe",
            "cwd": "${workspaceFolder}/build",
            "preLaunchTask": "CMake: build",
            "stopAtEntry": false,
            "externalConsole": false,
            "MIMode": "gdb"
        }
    ]
}